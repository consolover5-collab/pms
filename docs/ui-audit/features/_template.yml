# Template. Copy to features/NN-<slug>.yml and fill. Schema verified
# against pilot (03-booking-create). Validate with `pnpm --filter @pms/ui-audit validate-yaml`.

id: <section.slug>
title: "<human-readable title>"
route: /<path>
depends_on: []
locales_tested: [ru, en]

ui:
  header: { ru: "", en: "" }
  required_fields: []
  # Example required field (note is optional):
  # - { id: checkIn, label: { ru: "Заезд", en: "Check-in" }, kind: "date" }
  # - { id: rateAmount, label: { ru: "Цена / ночь", en: "Rate per Night" }, kind: "number", note: "auto-filled from rate plan × room type" }
  optional_fields: []
  # Example optional field (default is optional):
  # - { id: adults, label: { ru: "Взрослых", en: "Adults" }, kind: "number", default: 1 }
  # - { id: notes, label: { ru: "Примечания", en: "Notes" }, kind: "textarea" }
  buttons: {}
  # Example buttons map (keyed by semantic id):
  # submit: { ru: "Создать бронирование", en: "Create Booking" }
  # cancelAddGuest: { ru: "Отмена", en: "Cancel" }

steps: []
# Example step:
# - id: "01-empty-form"
#   action: "Open page under admin; form renders empty"
#   screenshots: [NN-slug-01-empty-form-ru.png, NN-slug-01-empty-form-en.png]

edge_cases: []
# Example edge case:
# - id: empty-submit
#   description: "Submit empty form"
#   expected: "HTML5 required blocks submit; no POST"
#   observed: "OK — screenshot 02"

api_calls_observed: []
# Example:
# - { method: GET,  path: "/api/properties" }
# - { method: POST, path: "/api/bookings", status: 201, note: "happy-path" }

console_errors: []
# Example:
# - locale: ru
#   scenario: room-unavailable
#   text: "Failed to load resource: 400"
#   verdict: expected

network_errors: []
# Example:
# - locale: ru
#   scenario: room-unavailable
#   status: 400
#   method: POST
#   url: "http://localhost:3000/api/bookings"
#   verdict: expected

bugs: []
# Example bug:
# - id: BUG-042
#   severity: low
#   scenario: empty-submit
#   observed: "Submit button stays enabled after click"
#   screenshot: NN-slug-02-empty-validation-ru.png

help_rewrite_hints:
  # Set topic to a help-article id (e.g. "bookings") or null if no matching help topic exists.
  topic: <help-topic-id-or-null>
  current: ""
  actual: ""
  rewrite: |

retro:
  time_minutes: 0
  unblocker_fixes: []
  methodology_deltas: []

status: pending   # ok | partial | broken | missing | pending
last_audited: null
screenshots_dir: docs/ui-audit/screenshots/
